{
  "name": "marantosws.github.io",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d dist",
    "test": "true"
  },
  "dependencies": {
    "@gridsome/plugin-google-analytics": "^0.1.0",
    "gh-pages": "^2.0.1",
    "gridsome": "^0.6.0"
  }
}
